server:
  port: 8080
  servlet:
    context-path: /metahelper
  
management:
    endpoint:
        beans:
            enabled: true
        env:
            enabled: true
        info:
            enabled: true
    endpoints:
        enabled-by-default: true
        web:
            exposure:
                include: '*' 